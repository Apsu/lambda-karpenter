apiVersion: v1
kind: Pod
metadata:
  name: gpu-test
spec:
  restartPolicy: Never
  containers:
    - name: gpu-sleep
      image: ubuntu:22.04
      command: ["bash", "-lc", "sleep 3600"]
      resources:
        limits:
          nvidia.com/gpu: 1
